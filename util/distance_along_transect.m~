function d = distance_along_transect(x_in,y_in)
% Finds distance between points along a transect

N = length(x_in);
d = zeros(length(x_in));
for i=2:N
    d(i) = d(i-1)+sqrt((x(i)-x(i-1))^2+((
end


end

